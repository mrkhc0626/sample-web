import React, { useState } from 'react';
import { Container } from '../../components';

export const Home = (props: any) => {
	return (
		<Container>
			<div className="flex-row home-page">Text</div>
		</Container>
	);
};
